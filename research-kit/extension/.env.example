VITE_API_URL=https://api.example.com/v1
VITE_GOOGLE_CLIENT_ID=
VITE_OPENCLAW_WS_URL=wss://api.example.com/ws
# For stable OAuth redirect URL during development, generate a manifest key:
# chrome://extensions -> Load unpacked -> Copy extension ID -> use crxmake or chrome to generate key
# MANIFEST_KEY=
